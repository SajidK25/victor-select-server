enum Role {
  ADMIN
  PATIENT
  PHYSICIAN
  VISITOR
}

type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  password: String!
  email: String! @unique
  stripeId: String @unique
  role: Role @default(value: VISITOR)
  count: Int @default(value: 0)
}
