enum Role {
  ADMIN
  PATIENT
  PHYSICIAN
}

type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  password: String!
  email: String! @unique
  stripeId: String @unique
  role: Role @default(value: PATIENT)
  count: Int @default(value: "0")
}
